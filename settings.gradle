pluginManagement {
    repositories {
        maven { url = 'https://maven.minecraftforge.net/' }
        maven { url = 'https://maven.fabricmc.net/' }
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'net.minecraftforge.gradle' version "${forge_gradle_version}"
        id 'fabric-loom' version "${fabric_loom_version}"
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'Template' //u can change

//RECOMMEND : write all code (mixins too) in common and in fabric and forge only for init and compile (P.s neoforge supported, quilt in version 0 not)
include 'common'
include 'fabric'
include 'forge'
